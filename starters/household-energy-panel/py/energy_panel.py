# Household Energy Panel
#
# Replace each `print(0)` below with the expression that produces its reading.
# The grader runs this program and compares what it prints, line by line, so
# each line must print exactly the right text and the panel must print nothing
# else. Do not add prompts or extra output.
#
# Two rules hold for every reading, both from the Values, Types, and Expressions
# lecture. An expression may not mix a whole number with a decimal number, so a
# reading that needs a fraction is written with every number as a float
# (100.0, not 100). And every division here divides floats, so both languages
# produce the same exact fraction.

print("ENERGY AUDIT")          # panel label (already written)

# TODO: annual energy use in kWh, 4500 W running 2.5 hours a day, over a full year
print(0)

# TODO: annual emissions in kg CO2e, at 275 g CO2e per kWh
print(0)

# TODO: typical-use flag: 1 if the annual energy use above sits above the
#       3,150 kWh figure, else 0. Work the comparison out by hand and write the
#       whole number it produces; comparing with > is not on the ladder yet.
print(0)

# TODO: meter status code: 4, plus 2 for each of 3 retries, minus 1
print(0)
