# Assignment 7 starter. The full specification is the assignment page.
#
# You write the test suite and nothing else. dashboard_cleared is supplied when
# your suite runs, so do not define it here: ./check hands your suite a correct
# implementation, which every assertion must hold against, and several broken
# ones, each of which at least one assertion must catch.
#
# The correct implementation is printed on the assignment page. Write your
# expected values from the specification, never from that code.


def test_dashboard_cleared():
    # TODO: assert one case per row of your Step 1 table, then print a
    #       confirmation line once every assertion has held.
    ...
