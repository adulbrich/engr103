# EV Carbon Payback
#
# This program reads four whole numbers from standard input, one per line, and
# prints exactly three whole numbers, one per line, and nothing else.
#
# It must not print anything before those three lines, including a prompt. Read
# every value with a bare input() call, with no prompt text: a printed prompt
# would make the two languages' output differ and break the comparison the
# grader does.

# TODO: read the four values below, one per line, with int(input())
extra_manufacturing_gco2 = 0
gasoline_per_km_gco2 = 0
ev_per_km_gco2 = 0
annual_km = 0

# TODO: replace each 0 below with the right computation from the variables above.
payback_km = 0
whole_years = 0
leftover_km = 0

print(payback_km)
print(whole_years)
print(leftover_km)
