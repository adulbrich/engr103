---
title: Glossary
description: One plain-language definition per course term, each linking to the lecture that explains it in full.
sidebar:
  order: 0
draft: true
---

{/* AUTHOR NOTE, not rendered to students.

Each term below is an `##` heading (giving it a stable anchor such as
`#function`), one plain-language sentence of definition, and a link to the
lecture section where the term is first introduced and explained in full. Other
artifacts reference a term by linking to its entry here (for example
`/reference/glossary#function`); this page routes the reader on to the lecture.

STATUS: 2 of roughly 80 terms authored. The full term list is the union of the
`glossary_terms` fields in the 18 lecture ai-summary blocks. This page is
`draft: true` until those are written, because a live glossary with two entries
is worse than none. Flip to false only once the terms are in and the lectures
link them from first use (VISION section 9's plain-language rule). */}

## console input

Text a program reads from the person running it while the program executes, rather than text already written into the source code. Introduced in [Variables and State](/lectures/variables-and-state/#console-input).

## parsing

Turning text into a value of another type, such as turning the text `"25"` into the whole number `25`. Introduced in [Errors, Input, and Validation](/lectures/errors-input-and-validation/#parsing).
